import { makeStyles, Typography } from '@material-ui/core'
import React from 'react'

const useStyles = makeStyles((theme) =>({
    
}))
const Tags = props =>{

    return(
        <div>
            <Typography> tags here</Typography>
        </div>
    )
}

export default Tags